; Astradial Dialplan Configuration
; Auto-managed by Astradial API — do not edit manually

[general]
static=yes
writeprotect=no
clearglobalvars=no

[globals]

; Default context — catches unrouted calls
[default]
exten => _X.,1,NoOp(Unrouted call to ${EXTEN})
same => n,Answer()
same => n,Playback(number-not-in-service)
same => n,Hangup()

; Organization-specific dialplans are auto-generated
; They are included via #include directives added by config deploy
