const express = require('express');
const router = express.Router();

// Placeholder routes for trunks
router.get('/', (req, res) => {
  res.json({ message: 'Trunks endpoint - not implemented yet' });
});

module.exports = router;